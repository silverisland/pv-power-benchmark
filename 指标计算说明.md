# 光伏功率预测指标计算说明

## 1. 适用范围

当前评估包含两个预测目标，均使用 15 分钟时间分辨率：

| 任务 | 起报规则 | 评估范围 | 点数 | 基础误差形式 |
| --- | --- | --- | ---: | --- |
| 超短期预测 | 每个有效起报时刻 | 起报后的连续 4 小时 | 16 | 归一化绝对误差均值 |
| 短期预测 | 每天上午 10:00 起报 | 第二天 00:00 至 23:45 | 96 | 归一化均方根误差 |

## 2. 符号定义

对一个起报样本，定义：

- \(P_i\)：第 \(i\) 个有效时刻的真实功率；
- \(\hat P_i\)：第 \(i\) 个有效时刻的预测功率；
- \(C\)：该样本对应的装机容量，必须大于 0；
- \(r=0.2\)：低功率保护比例；
- \(D_i=\max(P_i,rC)\)：第 \(i\) 个点的归一化分母。

功率和容量必须使用相同单位。当前接口不对最终准确率做 `[0, 1]` 截断，因此误差很大时准确率允许小于 0。

## 3. 超短期预测指标

超短期预测覆盖起报时刻后的连续 4 小时：

```text
T0+15min, T0+30min, ..., T0+4h
```

共 \(N_u=16\) 个点。单次起报的超短期归一化误差为：

\[
E_{u}
=
\frac{1}{16}
\sum_{i=1}^{16}
\frac{|\hat P_i-P_i|}{\max(P_i,0.2C)}
\]

超短期预测准确率为：

\[
\boxed{
A_{u}
=
1-
\frac{1}{16}
\sum_{i=1}^{16}
\frac{|\hat P_i-P_i|}{\max(P_i,0.2C)}
}
\]

百分数形式为 \(100A_u\%\)。

## 4. 短期预测指标

短期预测每天上午 10:00 起报，评估第二天全天：

```text
次日 00:00, 00:15, ..., 23:45
```

共 \(N_s=96\) 个点。单次起报的短期归一化均方根误差为：

\[
E_{s}
=
\sqrt{
\frac{1}{96}
\sum_{i=1}^{96}
\left(
\frac{\hat P_i-P_i}{\max(P_i,0.2C)}
\right)^2
}
\]

短期预测准确率为：

\[
\boxed{
A_{s}
=
1-
\sqrt{
\frac{1}{96}
\sum_{i=1}^{96}
\left(
\frac{\hat P_i-P_i}{\max(P_i,0.2C)}
\right)^2
}
}
\]

百分数形式为 \(100A_s\%\)。

## 5. 多次起报的汇总

先独立计算每次起报的误差与准确率，再对 \(M\) 次起报等权平均：

\[
\bar E=\frac{1}{M}\sum_{j=1}^{M}E_j,
\qquad
\bar A=\frac{1}{M}\sum_{j=1}^{M}A_j=1-\bar E
\]

这种汇总方式使每次起报的权重相同。接口同时返回每次起报的结果，便于进一步按场站、日期或月份分组。

## 6. 输入数据要求

| 字段 | 类型 | 超短期 | 短期 | 含义 |
| --- | --- | ---: | ---: | --- |
| `timestamp_win` | timestamp | 任意有效起报时刻 | 必须为每天 10:00 | 起报时刻 |
| `station` | string | 必填 | 必填 | 场站或省级对象 ID |
| `prediction` | list<float> | 长度 16 | 长度 96 | 预测功率 |
| `groundtruth` | list<float> | 长度 16 | 长度 96 | 真实功率 |
| `capacity` | float | 标量且大于 0 | 标量且大于 0 | 装机容量 |

预测和真实功率必须一一对应同一组有效时刻。输入不允许包含 `NaN` 或无穷值；接口遇到非法输入会抛出 `ValueError`，不会静默丢弃数据。

## 7. Python 接口

核心实现位于 `pv_metrics.py`，只依赖 NumPy。

### 7.1 单次起报

```python
from pv_metrics import short_term_metrics, ultra_short_metrics

ultra_result = ultra_short_metrics(
    groundtruth_16,
    prediction_16,
    capacity=100.0,
)

short_result = short_term_metrics(
    groundtruth_96,
    prediction_96,
    capacity=100.0,
)

print(ultra_result.accuracy)
print(short_result.accuracy_percent)
```

### 7.2 批量起报

`groundtruth` 和 `prediction` 可以传入 `(batch_size, horizon)` 二维数组，`capacity` 可以是一个公共标量，也可以是长度为 `batch_size` 的数组：

```python
from pv_metrics import evaluate_forecast

result = evaluate_forecast(
    task="short_term",
    groundtruth=groundtruth_batch,  # shape: (B, 96)
    prediction=prediction_batch,   # shape: (B, 96)
    capacity=capacity_batch,       # shape: (B,)
)

print(result.accuracy)
print(result.per_sample_accuracy)
print(result.as_dict())
```

### 7.3 DataFrame

```python
import pandas as pd
from pv_metrics import evaluate_prediction_frame

frame = pd.read_parquet("mock_data/short_term_predictions.parquet")
result = evaluate_prediction_frame(frame, task="short_term")
```

DataFrame 接口默认读取 `groundtruth`、`prediction` 和 `capacity` 三列。可以通过参数修改列名。
默认还会校验起报时间和目标区间；对于不带时间字段的中间数据，可以显式传入 `validate_schedule=False`。

### 7.4 命令行接口

```bash
python scripts/calculate_metrics.py \
  --task ultra_short \
  --input mock_data/ultra_short_predictions.parquet

python scripts/calculate_metrics.py \
  --task short_term \
  --input mock_data/short_term_predictions.parquet
```

## 8. Mock data

- `mock_data/ultra_short_predictions.parquet`：每行包含一段长度 16 的超短期预测和真实值；
- `mock_data/short_term_predictions.parquet`：每行起报时间为 10:00，预测和真实值长度均为 96，并额外记录 `target_start` 和 `target_end`；
- 两份数据均可直接传入 `evaluate_prediction_frame`。
