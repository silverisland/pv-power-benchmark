# 光伏功率预测数据说明

## 1. 文档范围

本文档统一说明当前光伏功率预测项目使用的数据。数据分为四类：场站元信息、15 分钟预测样本、模型预测结果、生产交付结果。EUPV 使用独立的 1 小时格式，见第 8 节。

标准交换格式为 Parquet，程序中读取为 `pandas.DataFrame`。除时间戳、标识和容量外，历史序列与未来序列均保存在 DataFrame 的数组列中。数组元素统一使用 `float32`。

## 2. 通用约定

| 项目 | 约定 |
| --- | --- |
| 时区 | `Asia/Shanghai`；Parquet 时间戳保存为无时区本地时间 |
| 15 分钟时间点 | `00:00, 00:15, 00:30, 00:45, ...` |
| 小时时间点 | EUPV ODS 使用整点；原始天气可能使用半点，入 ODS 时完成对齐 |
| 时间排序 | 文件内按 `timestamp_win` 或 `timestamp` 升序 |
| 数组顺序 | 从最早时刻到最晚时刻 |
| 历史数组末点 | 起报时刻之前最近一个已观测时间点 |
| 未来数组首点 | 起报时刻后的第一个有效时间点 |
| 缺失数值 | 使用 `NaN`，不使用 `-999` 等哨兵值 |
| 场站 ID | 保存为字符串；禁止依赖整数类型或从文件名推断 |
| 功率单位 | 建议统一为 `MW`；若源数据不是 MW，应在进入标准层前换算并记录 |
| 气象单位 | GHI：`W/m²`；TEMP：`°C`；WS：`m/s`；WD：度；PREC：`mm`；PWAT：`mm` |

数组与时间的关系为：

```text
历史数组: [..., T0-30min, T0-15min]
起报时刻: T0
未来数组: [T0+15min, T0+30min, ..., T0+N×15min]
```

如果某个上游系统使用“首点等于起报时刻”的定义，必须在字段元数据或 manifest 中显式声明，不能与本规范的数据混用。

## 3. 场站元信息

推荐文件名：`station_info.csv`。

| 字段 | 类型 | 必填 | 含义 |
| --- | --- | --- | --- |
| `plantid` | string | 是 | 稳定的场站 ID |
| `plantname` | string | 否 | 场站名称 |
| `plant_pointname` | string | 否 | 生产系统点名 |
| `province` | string | 是 | 所属省份 |
| `GCCAPACITY` | float64 | 是 | 装机容量，建议单位 MW，必须大于 0 |
| `LONGITUDE` | float64 | 是 | 经度，范围 `[-180, 180]` |
| `LATITUDE` | float64 | 是 | 纬度，范围 `[-90, 90]` |
| `timezone` | string | 是 | 当前统一为 `Asia/Shanghai` |

## 4. 15 分钟场站训练／推理样本

推荐文件名：`pv_samples_15min.parquet`。一行表示一个场站在一个起报时刻的完整预测样本。多个场站可以保存在同一个文件，也可以拆成 `station={station_id}.parquet`。

### 4.1 标准字段

| 字段 | Arrow/Pandas 类型 | 形状 | 必填 | 含义 |
| --- | --- | ---: | --- | --- |
| `timestamp_win` | timestamp | 标量 | 是 | 起报时刻 |
| `station` | string | 标量 | 是 | 场站 ID |
| `cap_power_on` | float64 | 标量 | 是 | 起报时刻使用的有效装机容量 |
| `observe_power` | list<float32> | 672 | 是 | 过去 7 天功率；短历史模型取末 96 点 |
| `observe_power_future` | list<float32> | 192 | 训练/评估必填 | 未来 2 天真实功率 |
| `GHI_solargis` | list<float32> | 672 | 是 | 历史 GHI |
| `GHI_solargis_future` | list<float32> | 192 | 是 | 起报时刻可获得的未来 GHI 预报 |
| `TEMP_solargis` | list<float32> | 672 | 是 | 历史温度 |
| `TEMP_solargis_future` | list<float32> | 192 | 是 | 起报时刻可获得的未来温度预报 |
| `WS_solargis_future` | list<float32> | 192 | 否 | 未来风速预报 |
| `WD_solargis_future` | list<float32> | 192 | 否 | 未来风向预报 |

推理数据可以不含 `observe_power_future`，其他字段与训练数据保持一致。

### 4.2 历史项目字段别名

| 标准字段 | 现有项目别名 | 说明 |
| --- | --- | --- |
| `timestamp_win` | `timestamp` | 需确认它表示起报时刻而非有效时刻 |
| `observe_power` | `Power` | 因子挖掘项目常用 |
| `observe_power_future` | `Power_predict` | 该字段是未来真实功率标签，不是模型预测 |
| `GHI_solargis` | `GHI_real`、`GHI_SOLARGIS` | `GHI_real` 表示实测历史 GHI，来源语义应保留 |
| `GHI_solargis_future` | `GHI_SOLARGIS_predict` | 未来气象预报 |
| `TEMP_solargis_future` | `TEMP_SOLARGIS_predict` | 未来气象预报 |
| `WS_solargis_future` | `WS_SOLARGIS_predict` | 未来气象预报 |
| `WD_solargis_future` | `WD_SOLARGIS_predict` | 未来气象预报 |

建议新数据使用标准字段；兼容旧模型时在读取适配层做重命名，不复制两套含义相同的列。

## 5. 省级样本

省级训练数据仍使用第 4 节的表结构，`station` 使用固定省级 ID，例如 `province_guangxi_solar`。同一个 `timestamp_win` 还可以包含多个场站行：

- 省级行提供 `observe_power`、`observe_power_future` 和省级容量；
- 场站行提供气象数组与 `cap_power_on`；
- 模型按有效装机容量聚合场站气象；
- 同一起报时刻的省级行应唯一。

省级超短期模型通常使用历史功率末 96 点，并分别训练未来第 1 至第 16 个 15 分钟时效。

## 6. 预测评估结果

推荐文件名：`predictions_15min.parquet`。一行表示一个模型对一个场站、一个起报时刻给出的完整预测曲线。

| 字段 | 类型 | 形状 | 必填 | 含义 |
| --- | --- | ---: | --- | --- |
| `timestamp_win` | timestamp | 标量 | 是 | 起报时刻 |
| `station` | string | 标量 | 是 | 场站或省级 ID |
| `model_id` | string | 标量 | 是 | 模型与版本标识 |
| `prediction` | list<float32> | 192 | 是 | 未来 2 天预测功率 |
| `groundtruth` | list<float32> | 192 | 评估必填 | 同一有效时刻上的真实功率 |
| `capacity` | float64 | 标量 | 是 | 计算归一化指标使用的容量 |

`prediction[i]` 与 `groundtruth[i]` 必须表示同一个有效时刻。禁止通过截断不同长度数组来静默对齐。

用于正式指标计算的任务数据还应裁剪为明确的评估范围：

- 超短期：`ultra_short_predictions.parquet`，连续未来 4 小时，数组长度 16；
- 短期：`short_term_predictions.parquet`，每天 10:00 起报、预测第二天全天，数组长度 96。

具体公式和接口见 [指标计算说明](指标计算说明.md)。

## 7. 生产交付结果

推荐文件名格式：

```text
hw_{YYYYMMDDHHMM}_{forecast_type}_province_{province}_solar_{model_version}.parquet
```

省级交付采用长表，一行一个目标有效时刻：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `dtime` | timestamp | 目标有效时刻 |
| `predict_power_province_guangxi_solar` | float64 | 省级预测功率 |

多场站生产宽表可以增加 `predict_power_plant_{pointname}` 列。交付表不保存数组，方便下游系统按有效时刻查询。

## 8. EUPV 小时级格式

EUPV 数据保持单独格式，不与 15 分钟数组直接拼接。一行表示一个站点在一个整点时刻的观测和未来 24 小时预报。

推荐文件名：`eupv_samples_hourly.parquet`。

| 字段 | 类型 | 形状 | 含义 |
| --- | --- | ---: | --- |
| `timestamp` | timestamp | 标量 | ODS 整点时间 |
| `id` | string | 标量 | 站点 ID |
| `pv_data` | float64 | 标量 | 当前时刻光伏功率 |
| `GHI`, `TEMP`, `WS`, `WD`, `PREC`, `PWAT` | float64 | 标量 | 当前气象 |
| `GHI_future1d` 等 | list<float32> | 24 | 未来 24 小时气象预报 |

预测结果推荐文件名：`eupv_predictions_hourly.parquet`，字段为 `timestamp`、`id`、`prediction`、`groundtruth`，其中两个数组长度均为 24。

## 9. 数据校验规则

入库或训练前至少执行以下检查：

1. 主键唯一：场站样本检查 `(station, timestamp_win)`；EUPV 检查 `(id, timestamp)`。
2. 时间戳非空且为 15 分钟或 1 小时边界。
3. 必填数组长度严格等于规范值，数组元素可以转为有限浮点数。
4. 历史功率、未来功率和气象数组内部按时间升序。
5. 功率原则上位于 `[0, 1.2 × capacity]`；超界值保留前先确认是否为数据异常。
6. `capacity > 0`，且站点 ID 可以在元信息表中找到。
7. GHI 夜间应接近 0，温度、风速、风向需满足合理物理范围。
8. 训练、验证和测试按起报时刻切分，不能随机打散相邻样本。
9. 预测数组与真实数组的长度、首个有效时刻和时间步长完全一致。

## 10. Mock data

`mock_data/` 中的数据由 `scripts/generate_mock_data.py` 固定随机种子生成，只用于接口联调和测试，不代表真实场站分布。执行：

```bash
python scripts/generate_mock_data.py
```

生成器默认创建：

- `station_info.csv`
- `pv_samples_15min.parquet`
- `predictions_15min.parquet`
- `ultra_short_predictions.parquet`
- `short_term_predictions.parquet`
- `province_delivery.parquet`
- `eupv_samples_hourly.parquet`
- `eupv_predictions_hourly.parquet`
- `manifest.json`

`manifest.json` 记录文件、行数、字段、数组长度、时间分辨率、单位和随机种子，便于自动化检查。
